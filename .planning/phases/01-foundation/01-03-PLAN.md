---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - data/output_a.md
  - data/output_b.md
autonomous: true
requirements:
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "data/output_a.md is a model NDA review that correctly identifies all 4 issues including judgment calls"
    - "data/output_b.md is a flawed review that nails extraction but misses judgment"
    - "Output A would score 2/2 on all 8 rubric items"
    - "Output B would score 2/2 on extraction items (1a, 2a, 3a, 4a) and 0 or 1 on judgment items (1b, 2b, 3b, 4b)"
  artifacts:
    - path: "data/output_a.md"
      provides: "Model NDA review - all issues correctly identified with judgment"
      min_lines: 30
    - path: "data/output_b.md"
      provides: "Flawed NDA review - extraction correct, judgment missed"
      min_lines: 30
  key_links:
    - from: "data/output_a.md"
      to: "data/nda.md"
      via: "References specific NDA clauses and issues"
      pattern: "confidentiality period|non-solicitation|independently"
    - from: "data/output_b.md"
      to: "data/nda.md"
      via: "References same clauses but misses judgment significance"
      pattern: "confidentiality period|non-solicitation|independently"
---

<objective>
Author Output A (model review) and Output B (flawed review) -- the two pre-written NDA reviews used in the Phase 3 pre-loop judge test. These must interlock precisely with the NDA, rubric, and playbook from Plan 02.

Purpose: The pre-loop judge test (Phase 3) runs both outputs through the judge to verify it can distinguish quality. Output A must be unambiguously strong on all 8 items. Output B must nail extraction perfectly but miss judgment -- this is the thesis-critical signal.

Output: Two review files that, when scored against the rubric/playbook, produce a measurable gap on judgment items.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@prd.md

@data/nda.md
@data/rubric.json
@data/playbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Output A (model review) and Output B (flawed review)</name>
  <files>data/output_a.md, data/output_b.md</files>
  <read_first>
    data/nda.md
    data/rubric.json
    data/playbook.md
    prd.md
    .planning/phases/01-foundation/01-CONTEXT.md
    .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
Read the NDA, rubric, and playbook carefully FIRST. Both outputs must reference the actual content of the NDA (clause numbers, specific language). Do NOT write generic reviews -- they must map precisely to the NDA that was authored in Plan 02.

**data/output_a.md -- Model NDA Review (DATA-04):**

Write a structured NDA review that would score 2/2 on ALL 8 rubric items. Format as a structured list with, for each issue: the clause reference, a description of the issue, and a risk assessment (per PRD section 3.4 agent output format).

For each of the 4 issues, Output A must:
- **Issue 1 (extraction + judgment):** Explicitly identify the 7-year confidentiality period AND provide a substantive assessment of how this compares to market norms (2-3 years) and the practical implications of the extended term.
- **Issue 2 (extraction + judgment):** Quote or specifically describe the overbroad definition scope (business, operations, customers, technology, strategy) AND provide a meaningful analysis of the negotiation risk this breadth creates.
- **Issue 3 (extraction + judgment):** Identify the 24-month non-solicitation obligation AND its location in the remedies section, AND provide a substantive assessment of why this placement is unusual and how the obligation extends beyond typical NDA scope.
- **Issue 4 (extraction + judgment):** Identify the independently-developed-information exception AND describe the specific onerous requirements, AND provide a substantive assessment of how these requirements render the exception functionally unusable.

Use clause references from the actual NDA (e.g., "Section 3" or "Clause 7" -- match the NDA's numbering).

**data/output_b.md -- Flawed NDA Review (DATA-05):**

Write a structured NDA review that would score 2/2 on extraction items (1a, 2a, 3a, 4a) but 0 or 1 on judgment items (1b, 2b, 3b, 4b). Same structural format as Output A.

For each of the 4 issues, Output B must:
- **Issue 1 (extraction=2, judgment=0-1):** Identify the 7-year period correctly but either don't compare to market norms at all, or mention it in passing without substantive analysis.
- **Issue 2 (extraction=2, judgment=0-1):** Describe the broad definition accurately but conclude it is "comprehensive" or "thorough" rather than flagging it as a negotiation risk.
- **Issue 3 (extraction=2, judgment=0-1):** Find the non-solicitation clause and note the 24-month term, but DON'T flag the unusual placement in remedies or that it extends beyond NDA scope. Treat it as a standard provision.
- **Issue 4 (extraction=2, judgment=0-1):** Describe the independently-developed-information exception and mention the requirements, but conclude the exception is "standard" or "provides adequate protection" rather than recognising it's functionally unusable.

CRITICAL PITFALL (from Research): Output B must NOT accidentally fail on extraction items. After drafting, cross-check every extraction rubric item (1a, 2a, 3a, 4a) against the playbook's Score 2 criteria to ensure Output B explicitly satisfies each one.

Both reviews should use the neutral perspective (D-05) -- identify issues for either party, not from a stated side.
  </action>
  <verify>
    <automated>cd /Users/houfu/Projects/judge-is-the-ceiling && python3 -c "
a = open('data/output_a.md').read()
b = open('data/output_b.md').read()
# Both must reference key issues
for keyword in ['7-year', 'confidential', 'non-solicitation', 'independently']:
    kw_lower = keyword.lower()
    assert kw_lower in a.lower(), f'Output A missing: {keyword}'
    assert kw_lower in b.lower(), f'Output B missing: {keyword}'
# Output A must have judgment language
for judgment_word in ['market norm', 'negotiation risk', 'unusual', 'unusable']:
    jw_lower = judgment_word.lower()
    assert jw_lower in a.lower(), f'Output A missing judgment signal: {judgment_word}'
# Output B should NOT have strong judgment language (at least some missing)
judgment_signals = ['unusable', 'beyond nda scope', 'unusual placement']
found_judgment = sum(1 for j in judgment_signals if j in b.lower())
assert found_judgment <= 1, f'Output B has too many judgment signals ({found_judgment}/3) -- should miss judgment'
# Both should be substantial
assert len(a.split()) >= 300, f'Output A too short: {len(a.split())} words'
assert len(b.split()) >= 300, f'Output B too short: {len(b.split())} words'
print(f'Output A: {len(a.split())} words, Output B: {len(b.split())} words')
print(f'Output B judgment signals found: {found_judgment}/3 (should be 0-1)')
print('OUTPUTS OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - data/output_a.md exists and is >= 300 words
    - data/output_b.md exists and is >= 300 words
    - Both files reference the 7-year confidentiality period
    - Both files reference confidential information definition
    - Both files reference non-solicitation
    - Both files reference independently-developed-information exception
    - Output A contains "market norm" (or equivalent judgment language about duration)
    - Output A contains "negotiation risk" (or equivalent about definition scope)
    - Output A contains "unusual" (about non-solicitation placement)
    - Output A contains "unusable" (or equivalent about gutted carve-out)
    - Output B does NOT contain "unusable" or "functionally unusable" regarding the carve-out exception
    - Output B does NOT flag non-solicitation placement as "unusual" or "beyond NDA scope"
    - Output B correctly identifies all 4 issues at the extraction level (finds the clauses, describes the content)
  </acceptance_criteria>
  <done>Output A is a model review scoring 2/2 on all 8 rubric items. Output B nails extraction (2/2 on 1a, 2a, 3a, 4a) but misses judgment (0-1 on 1b, 2b, 3b, 4b). Both reference actual NDA clause numbers and content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None | Static content files with no runtime trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | data/ files | accept | Static experiment inputs; single researcher; no external consumers |
</threat_model>

<verification>
1. Both output files exist and are substantial (>= 300 words each)
2. Output A demonstrates all 4 judgment calls (market norms, negotiation risk, unusual placement, functionally unusable)
3. Output B correctly extracts all 4 issues but lacks judgment substance
4. Both reference actual NDA clause numbers from data/nda.md
5. Cross-check Output B against extraction rubric items (1a, 2a, 3a, 4a) playbook Score 2 criteria
</verification>

<success_criteria>
- Output A would score 2/2 on all 8 rubric items when evaluated against playbook
- Output B would score 2/2 on extraction (1a, 2a, 3a, 4a) and 0-1 on judgment (1b, 2b, 3b, 4b)
- Both outputs reference actual NDA content (clause numbers, specific language)
- The gap between Output A and Output B is entirely on judgment items -- extraction performance is identical
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
